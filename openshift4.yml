classes:
  - .common

parameters:
  prometheus:
    namespaces:
      syn-monitoring:
        annotations:
          openshift.io/node-selector: ""
    prometheusOperator:
      installCRDs: false
    addons:
      - openshift4
